[user]
	name = TSomKes
	email = tsomkes@gmail.com
[github]
	user = TSomKes
	token = !cat ~/.githubtoken 
[core]
	editor = vim
	excludesfile = ~/.gitignore
[alias]
	diffz = diff --color=auto
	logz = log --pretty=format:\"%h - %an, %ar : %s\"
	logg = log --graph --pretty=format:'%h - %ad %an | %s' --date=short
	statz = status -sb
[color]
	ui = auto

[push]
	default = simple
